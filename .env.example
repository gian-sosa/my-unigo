# Environment Variables Required for Production

# Supabase Configuration
VITE_APP_SUPABASE_URL=https://jyaccaqwurmztjsjlacb.supabase.co
VITE_APP_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Notes:
# - These variables MUST be set in your deployment platform (Netlify, Vercel, etc.)
# - For local development, copy this file to .env.local and fill in the actual values
# - The anon key is safe to expose in frontend applications (it's designed for client-side use)
# - Actual security is handled by Supabase Row Level Security (RLS)
